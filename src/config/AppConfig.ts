import { IRouter } from "@/airpower/interface/IRouter";

export class AppConfig {

    static appId: string = 'SPower4React'

    static fieldEmptyHolder: string = '--'

    static routes: IRouter[]
}